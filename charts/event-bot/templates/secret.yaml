{{- if .Values.vault.enable }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: gslb-event-bot-secrets
  namespace: {{ .Values.namespace }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: gslb-event-bot-secret-store				
    kind: SecretStore
  target:
    name: gslb-event-bot-secrets
  data:
    {{- range .Values.vault.secretMappings }}
    - secretKey: {{ .secretKey }}
      remoteRef:
        key: gslb-event-bot
        property: {{ .property }}
    {{- end }}
    {{- if .Values.settings.mq_enabled }}
    {{- range .Values.vault.mqSecretMappings }}
    - secretKey: {{ .secretKey }}
      remoteRef:
        key: gslb-event-bot
        property: {{ .property }}
    {{- end }}
    {{- end }}
{{- end }}